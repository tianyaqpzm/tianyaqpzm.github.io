
# python
[iPython的安装及问题解决](https://www.jianshu.com/p/8779eac7f313)
[jupyter安装多个python环境](https://blog.csdn.net/djshichaoren/article/details/79198102)

[在Jupyter中安装Python包](https://blog.csdn.net/wja8a45TJ1Xa/article/details/78757258)


python -m pip install -upgrade pip  更新pip 源

在sudoers 里添加用户和权限
mkdir -m 777 test3

```
cat >hello.py << EOF
#!/usr/bin/env python
#-*- coding:utf-8 -*-
__authon__=''
if __name__='main':
    print(u"你好，python")
EOF

cd !$     去 !$:表示上一次使用的路径。


windows 默认GBK编码
#!/usr/bin/python env  windows 不需要
#coding=utf-8
#-*- coding:GBK -*-
linux 默认utf-8
```
赋值自动识别<= 2^63-1 = 9223372036854775807  int,
float 不包括无限小数，不区分精度
a+bj
二进制 bin(-1000) 
八进制 oct()
十六进制 hex()


linux vi :
set ts=4
set expandtab

匈牙利命名法： 属性+类型+对象

[urllib与urllib2的学习总结(python2.7.X)](https://www.cnblogs.com/wly923/archive/2013/05/07/3057122.html)
[python中urllib, urllib2,urllib3, httplib,httplib2, request的区别](https://blog.csdn.net/permike/article/details/52437492)
python 3.x中urllib库和urilib2库合并成了urllib库。。
其中urllib2.urlopen()变成了urllib.request.urlopen()
urllib2.Request()变成了urllib.request.Request()


原因查询源码 (python3.5.2) ，urllib moudle中并没有request相关方法,直接是urlopen方法


urllib.request 用于打开和读取URL, 
urllib.error 用于处理前面request引起的异常, 
urllib.parse 用于解析URL, 
urllib.robotparser用于解析robots.txt文件





### virtualenv

virtualenv -p ~/pyenv/python3.5 env3.5 来指定创建一个3.5的env







### Map函数的使用以及 input 的输入 sort 以k 排序

def fun8():

​	d = {}

​	for _ in range(input()):

​		k, v = map(int, raw_input().split())

​		d[k] = d.get(k, 0) + v

​	print '\n'.join(map(lambda x: '%d %d'% (x[0], x[1]), sorted(d.items(), key=lambda x: x[0])))

while True:

​	try:

​		fun8()

​	except EOFError:

​		break





求一个数 每位数：



while n>0:

​	sum +=(n%10)*(n%10)

​	n/=10



while True:

  try:

​    \#将输入的字符 组成一个列表 好经典啊啊 啊啊 啊啊啊

​    nums = [int(raw_input()) for _ in range(input())]

​    \#第一个参数 传 函数名

​    print '\n'.join(map(str, sorted(set(nums))))

  except EOFError:

​    break



\>>> import sys

\>>> message = sys.stdin.readlines()

Hello

World

My

Name

Is

James

Bond

\# <ctrl-d> EOF sent

\>>> print message

['Hello\n', 'World\n', 'My\n', 'Name\n', 'Is\n', 'James\n', 'Bond\n']





###  sort函数

sort是在原位重新排列列表，而sorted()是产生一个新的列表。

sorted(...)

  sorted(iterable, cmp=None, key=None, reverse=False) --> new sorted list

---------------------------------sort----------------------------------------

\>>> help(list.sort)

Help on method_descriptor:



sort(...)

  L.sort(cmp=None, key=None, reverse=False) -- stable sort *IN PLACE*;

  cmp(x, y) -> -1, 0, 1

\-----------------------------------------------------------------------------



iterable：是可迭代类型;

cmp：用于比较的函数，比较什么由key决定,有默认值，迭代集合中的一项;

key：用列表元素的某个属性和函数进行作为关键字，有默认值，迭代集合中的一项;

reverse：排序规则. reverse = True 或者 reverse = False，有默认值。

返回值：是一个经过排序的可迭代类型，与iterable一样。

 

注；一般来说，cmp和key可以使用**lambda****表达式**。





